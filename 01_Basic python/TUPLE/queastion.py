# Create a tuple with 5 elements and print it d-> p1 
# Access the first and last element of a tuple d->p2
# Slice a tuple to get the middle elements d->p3
# Check if an element exists in a tuple  
# Find the length of a tuple 
# Count how many times an element appears in a tuple d->p4
# Find the maximum and minimum element in a tuple d 
# Convert a tuple into a list d
# Convert a list into a tuple d->p5
# Concatenate two tuples d->p6


# Unpack a tuple into multiple variables d->p7
# Swap two variables using a tuple d->p8
# Iterate through a tuple and print elements d
# Find the sum of all elements in a tuple d
# Remove duplicates from a tuple d
# Find the index of a given element
# Reverse a tuple
# Sort a tuple (ascending & descending)
# Find the second largest element